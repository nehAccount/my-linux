server {
    listen 80;
    listen [::]:80;

    server_name docker.foodnotify.com;


    error_page 497 https://$host:$server_port$request_uri;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;
    rewrite_log on;

    # Enable gzip but do not remove ETag headers
    gzip on;
    gzip_vary on;
    gzip_comp_level 4;
    gzip_min_length 256;
    gzip_proxied expired no-cache no-store private no_last_modified no_etag auth;
    gzip_types application/atom+xml application/javascript application/json application/ld+json application/manifest+json application/rss+xml application/vnd.geo+json application/vnd.ms-fontobject application/x-font-ttf application/x-web-app-manifest+json application/xhtml+xml application/xml font/opentype image/bmp image/svg+xml image/x-icon text/cache-manifest text/css text/plain text/vcard text/vnd.rim.location.xloc text/vtt text/x-component text/x-cross-domain-policy;

    #add_header Strict-Transport-Security "max-age=15768000; includeSubDomains; always;";
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Robots-Tag none;
    add_header X-Download-Options noopen;
    add_header X-Permitted-Cross-Domain-Policies none;
    add_header X-Frame-Options sameorigin;
    add_header Referrer-Policy no-referrer;

    root /var/www/web;

    # Add index.php to the list if you are using PHP
    index app_dev.php;

    location /media {
        root /var/www/data/roaming;

        location ~ \.(?:png|html|jpg|jpeg|svg|gif|css|js\.map)$ {
            # add_header Cache-Control "public, max-age=15778463";
            try_files $uri =404;
        }

    }

    location ~ /\.ht {
        deny all;
    }

    location ~ \.(?:css\.map)$ {
        try_files $uri =404;
    }

    location ~ \.(?:png|html|ttf|ico|jpg|jpeg|svg|gif|css|js|woff|woff2|json|js\.map)$ {
        add_header Cache-Control "public, max-age=15778463";
        try_files $uri =404;
    }

    location / {
        try_files $uri /app_dev.php$is_args$args;
    }

    location ~ ^/app_dev\.php(/|$) {
        fastcgi_pass app:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
       # When you are using symlinks to link the document root to the
       # current version of your application, you should pass the real
       # application path instead of the path to the symlink to PHP
       # FPM.
       # Otherwise, PHP's OPcache may not properly detect changes to
       # your PHP files (see https://github.com/zendtech/ZendOptimizerPlus/issues/126
       # for more information).
       fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
       fastcgi_param DOCUMENT_ROOT $realpath_root;
       # Prevents URIs that include the front controller. This will 404:
       # http://domain.tld/app.php/some-path
       # Remove the internal directive to allow URIs like this
        fastcgi_read_timeout 7200;
#        fastcgi_param   SCRIPT_NAME        $fastcgi_script_name;
#        fastcgi_split_path_info ^(.+\.php)(/.)$;
#        fastcgi_param PATH_INFO $fastcgi_path_info;
#        fastcgi_param HTTPS on;
#        fastcgi_param modHeadersAvailable true;
#        fastcgi_param front_controller_active true;
#        fastcgi_pass php-handler;
#        fastcgi_intercept_errors on;
#        fastcgi_request_buffering off;
        internal;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        fastcgi_send_timeout 300;
        fastcgi_connect_timeout 300;
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
        proxy_read_timeout 7200;
   }

#     location ~ ^/app\.php {
#         fastcgi_pass unix:/run/php/php7.2-fpm.sock;
#         fastcgi_split_path_info ^(.+\.php)(/.)$;
#         include         fastcgi_params;
#         fastcgi_param   SCRIPT_FILENAME    $document_root$fastcgi_script_name;
#         fastcgi_param   DOCUMENT_ROOT      $realpath_root;
#         fastcgi_param   HTTPS              on;
#         fastcgi_read_timeout 7200;
# #        fastcgi_param   SCRIPT_NAME        $fastcgi_script_name;
# #        fastcgi_split_path_info ^(.+\.php)(/.)$;
# #        fastcgi_param PATH_INFO $fastcgi_path_info;
# #        fastcgi_param HTTPS on;
# #        fastcgi_param modHeadersAvailable true;
# #        fastcgi_param front_controller_active true;
# #        fastcgi_pass php-handler;
# #        fastcgi_intercept_errors on;
# #        fastcgi_request_buffering off;
#         internal;
#         fastcgi_buffers 16 16k;
#         fastcgi_buffer_size 32k;
#         fastcgi_send_timeout 300;
#         fastcgi_connect_timeout 300;
#         proxy_buffer_size 128k;
#         proxy_buffers 4 256k;
#         proxy_busy_buffers_size 256k;
#         proxy_read_timeout 7200;
#     }

    location ~ \.php$ {
        return 404;
    }
}

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;

#     server_name docker.foodnotify.com;

#     # allow 80.109.191.10;
#     # allow 212.247.176.200;
#     # deny all;


#     error_page 497 https://$host:$server_port$request_uri;

#     access_log on;
#     error_log on;
#     rewrite_log on;

#     ssl on;
#     ssl_ecdh_curve secp384r1;
#     ssl_trusted_certificate /etc/ssl/my.foodnotify.com/foodnotify.com.ca.pem;
#     ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
#     ssl_prefer_server_ciphers on;
#     ssl_certificate_key /etc/ssl/my.foodnotify.com/foodnotify.com.key.pem;
#     ssl_certificate /etc/ssl/my.foodnotify.com/foodnotify.com.pem;
#     #ssl_ciphers "ECDHE-RSA-AES256-GCM-SHA512(teeth)HE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384(teeth)HE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384";
#     ssl_ciphers 'ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-SHA384:ECDHE-RSA-AES256-SHA384:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA256';
#     server_tokens off;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:50m;
#     ssl_session_tickets off;
#     #ssl_stapling on;
#     #ssl_stapling_verify on;
#     resolver 8.8.8.8 8.8.4.4 valid=300s;
#     resolver_timeout 5s;

#     # Enable gzip but do not remove ETag headers
#     gzip on;
#     gzip_vary on;
#     gzip_comp_level 4;
#     gzip_min_length 256;
#     gzip_proxied expired no-cache no-store private no_last_modified no_etag auth;
#     gzip_types application/atom+xml application/javascript application/json application/ld+json application/manifest+json application/rss+xml application/vnd.geo+json application/vnd.ms-fontobject application/x-font-ttf application/x-web-app-manifest+json application/xhtml+xml application/xml font/opentype image/bmp image/svg+xml image/x-icon text/cache-manifest text/css text/plain text/vcard text/vnd.rim.location.xloc text/vtt text/x-component text/x-cross-domain-policy;

#     #add_header Strict-Transport-Security "max-age=15768000; includeSubDomains; always;";
#     add_header X-Content-Type-Options nosniff;
#     add_header X-XSS-Protection "1; mode=block";
#     add_header X-Robots-Tag none;
#     add_header X-Download-Options noopen;
#     add_header X-Permitted-Cross-Domain-Policies none;
#     add_header X-Frame-Options sameorigin;
#     add_header Referrer-Policy no-referrer;

#     root /var/www/my.foodnotify.com/web;

#     # Add index.php to the list if you are using PHP
#     index app.php;

#     location ~ /\.ht {
#         deny all;
#     }

#     location ~ \.(?:css\.map)$ {
#         try_files $uri =404;
#     }

#     location ~ \.(?:png|html|ttf|ico|jpg|jpeg|svg|gif|css|js|woff|woff2|json|js\.map)$ {
#         add_header Cache-Control "public, max-age=15778463";
#         try_files $uri =404;
#     }

#     location / {
#         try_files $uri /app.php$is_args$args;
#     }

#     location ~ ^/app\.php {
#         fastcgi_pass unix:/run/php/php7.2-fpm.sock;
#         fastcgi_split_path_info ^(.+\.php)(/.)$;
#         include         fastcgi_params;
#         fastcgi_param   SCRIPT_FILENAME    $document_root$fastcgi_script_name;
#         fastcgi_param   DOCUMENT_ROOT      $realpath_root;
# #        fastcgi_param   SCRIPT_NAME        $fastcgi_script_name;
# #        fastcgi_split_path_info ^(.+\.php)(/.)$;
# #        fastcgi_param PATH_INFO $fastcgi_path_info;
# #        fastcgi_param HTTPS on;
# #        fastcgi_param modHeadersAvailable true;
# #        fastcgi_param front_controller_active true;
# #        fastcgi_pass php-handler;
# #        fastcgi_intercept_errors on;
# #        fastcgi_request_buffering off;
# 	fastcgi_read_timeout 300;
#         internal;
#     }

#     location ~ \.php$ {
#         return 404;
#     }
# }